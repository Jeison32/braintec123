@model IEnumerable<braintec.Infrastructure.Data.Tcategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categoriarepuesto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categoriarepuesto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idcategoria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idcategoria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idcategoria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
